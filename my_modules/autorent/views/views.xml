<odoo>
  <data>
    <!-- explicit list view definition -->
<!-- 
    <record model="ir.ui.view" id="autorent.list">
      <field name="name">autorent list</field>
      <field name="model">autorent.autorent</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="description"/>
        </tree>
      </field>
    </record> -->


    <!-- actions opening views on models -->

    <record model="ir.actions.act_window" id="autorent.action_window">
      <field name="name">autorent window</field>
      <field name="res_model">autorent.autorent</field>
      <field name="view_mode">tree,form</field>
    </record>


    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="autorent.action_server">
      <field name="name">autorent server</field>
      <field name="model_id" ref="model_autorent_autorent"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->

    <menuitem name="AutoRent" id="autorent.menu_root"/>

    <!-- menu categories -->

    <!-- <menuitem name="AutoRent" id="autorent.menu_1" action='autorent.list' parent="autorent.menu_root"/> -->
    <menuitem name="AutoRent 2" id="autorent.menu_2" action="autorent.action_window" parent="autorent.menu_root"/>

    <!-- actions -->
<!--
    <menuitem name="List" id="autorent.menu_1_list" parent="autorent.menu_1"
              action="autorent.action_window"/>
    <menuitem name="Server to list" id="autorent" parent="autorent.menu_2"
              action="autorent.action_server"/>
-->
  </data>
</odoo>